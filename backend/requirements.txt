# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Database (SQLAlchemy + Postgres driver)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0            # Postgres async driver (required — set DATABASE_URL in .env)
psycopg2-binary>=2.9.0      # Postgres sync driver (required for sync SQLAlchemy engine)
alembic>=1.13.0

# Config
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# HTTP client (LLM calls, outbound requests)
httpx>=0.28.0

# Utilities
orjson>=3.10.0             # fast JSON serialization
python-dateutil>=2.9.0
structlog>=24.0.0          # structured logging
numpy>=1.24.0              # numerical computation
scikit-learn>=1.2.0        # HDBSCAN clustering and PCA

